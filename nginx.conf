server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: serve the file if it exists, otherwise fall back to
    # index.html so client-side routes (react-router) resolve correctly.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
